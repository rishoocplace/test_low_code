/**
 * Component which maps a date value.
 * @extends Component
 * @since 5.0
 * @hideconstructor
 * @category Office-Reporting
 */
class DateComponent {
    /**
     * Sets the date of the component to the given value. Both javascript and Joda DateTime objects can be used to provide value.
     * @param {Date|DateTime} date - The value to set. Can be a JS Date or a DateTime object.
     * @return {DateComponent} The component itself.
     */
    withDate(date) {
    }
}