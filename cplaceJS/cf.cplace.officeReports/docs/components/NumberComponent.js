/**
 * Object representing number in the report. Numbers can be added directly using {@link ComponentContainer#put()} method.
 * @extends Component
 * @since 5.0
 * @hideconstructor
 * @category Office-Reporting
 */
class NumberComponent {
    /**
     * Sets the numerical display of the component to the given value.
     * @param {number} number - The number to set.
     * @return {NumberComponent} The component itself.
     */
    withNumber(number) {
    }
}