/**
 * Object representing a slide in the powerpoint report
 * @extends ComponentContainer
 * @since 5.0
 * @hideconstructor
 * @category Office-Reporting
 */
class Slide {
    /**
     * Creates a clone of the shape identified by the key and applies the component to it.
     *
     * Clones should be moved as they appear at the position of the "donor" shape.
     * @param {string} key - The key of the shape to clone.
     * @param {Component} component - The component data to apply to the clone.
     */
    putClone(key, component) {}
}